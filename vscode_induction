how you will enable git in your vs Code
--first search "github" in the extension then download it ad=fter download it just go the account and choose git hub than do the authentication process .after that create the initialize repo using a empty folder in  the file system.
adding more information into it